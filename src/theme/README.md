# Theme

Design tokens live here once a visual direction is chosen. Three candidates are on the
canvas — thermal, avant, and colour fields — and the decision is recorded in an ADR when
it is made.

Until then this folder stays empty on purpose: tokens written against an undecided
direction get rewritten, and half-applied tokens are worse than none.
