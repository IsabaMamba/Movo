# Architecture decision records

One file per decision that was expensive to make and would be expensive to reverse.
Numbered, newest last, never deleted — a superseded ADR gets a `Superseded by` line
rather than a rewrite, because the reasoning that was wrong is part of the record.

Template: Status · Context · Decision · Consequences · Alternatives considered.
